# NABU CLI config — auto-loaded por ~/nabu-chat/nabu wrapper
# NO edites NABU_API_URL / SUPABASE_URL — son fijas del ecosistema NABU.
# SÍ tenés que pegar 2 credenciales que Daniel te pasa privado.

# === FIJAS — no tocar ===
export NABU_API_URL="https://api.calderon.coffee"
export SUPABASE_URL="https://dgrgwyrkvnrhlnrifdoj.supabase.co"

# === TUYAS — Daniel te las pasa por WhatsApp privado ===
# Anthropic API key (Claude Code la usa, NO necesitás subscripción Pro/Max):
# export ANTHROPIC_API_KEY="sk-ant-..."

# Internal token para invoke directo de agentes (POST /api/agent-invoke):
# export INTERNAL_SVC_TOKEN="..."
