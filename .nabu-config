# NABU API + Supabase config para esta sesión Claude Code
# Source este archivo: source .nabu-config

# Public NABU API URL (cloudflared tunnel — Daniel actualiza si cambia)
export NABU_API_URL="https://api.calderon.coffee"

# Supabase para queries directas (opcional, agente puede leer handoffs detallados)
export SUPABASE_URL="https://dgrgwyrkvnrhlnrifdoj.supabase.co"
# SUPABASE_SERVICE_ROLE_KEY: pedir a Daniel y agregar local (NO commitear)
# export SUPABASE_SERVICE_ROLE_KEY="..."

# Internal token para invoke directo de agentes (POST /api/agent-invoke)
# Pedir a Daniel — token rota; vive en GCP Secret Manager (internal-svc-token)
# export INTERNAL_SVC_TOKEN="..."
